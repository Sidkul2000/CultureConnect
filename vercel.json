{
  "version": 2,
  "buildCommand": "cd backend && npm install && npx prisma generate",
  "installCommand": "npm install --prefix backend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
